# mapReduce
MapReduce Implementation
## Compilation
Compile files using your compilator
Example:
`g++ -std=c++11 -o reduce reduce.cpp`

## Run
To run use terminal
Example:
`./map < test.txt |  ./reduce`
